services:
  web:
    image: ghcr.io/meror96/infportal:latest   
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DJANGO_SETTINGS_MODULE: infportal.settings
      # SECRET_KEY, DEBUG, DATABASE_URL — лучше через GitHub Secrets
